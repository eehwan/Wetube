header.header
  .header__wrapper
    .header__column
      a(href=routes.home)
        <i class="fab fa-youtube"></i>WeTube
    .header__column
      form(action=routes.search, method="get")
        input(type= "text", placeholder="Search...", name="term", autocomplete="off")
    .header__column
      ul
        if !user
          li
            a(href=routes.join) Join
          li
            a(href=routes.login) LogIn
        else
          li
            a(href=routes.videos+routes.upload) Upload
          li
            a(href=routes.users+routes.userDetail(user.id)) Profile
          li
            a(href=routes.logout) LogOut
